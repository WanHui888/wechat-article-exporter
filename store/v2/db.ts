// db.ts is no longer used — storage has been migrated from Dexie (IndexedDB)
// to server-side SQLite via /api/store/* endpoints.
// This file is kept as an empty shim to avoid breaking any remaining type imports.

export {}
